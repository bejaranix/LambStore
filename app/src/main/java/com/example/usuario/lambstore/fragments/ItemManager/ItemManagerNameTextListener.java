package com.example.usuario.lambstore.fragments.ItemManager;

/**
 * Listens the text written in nameET field.
 */
public interface ItemManagerNameTextListener {

    /**
     * Listens when text has been changed and gets the text itself.
     * @param {@link String} text, the text changed.
     */
    void onTextChanged(String text);
}


